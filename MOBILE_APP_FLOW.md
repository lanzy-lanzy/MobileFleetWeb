# 📱 Mobile Fleet App - Complete Flow Diagram

## 🎯 System Overview

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          MOBILE FLEET MONITORING SYSTEM                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│  │   KOTLIN APP    │◄──►│ DJANGO REST API │◄──►│ FIREBASE STORE  │         │
│  │   (Mobile)      │    │   (Backend)     │    │   (Database)    │         │
│  └─────────────────┘    └─────────────────┘    └─────────────────┘         │
│           │                        │                        │               │
│           │                        │                        │               │
│           └────────────────────────┼────────────────────────┘               │
│                                    │                                        │
│                              ┌─────────────────┐                           │
│                              │ WEB DASHBOARD   │                           │
│                              │  (Real-time)    │                           │
│                              └─────────────────┘                           │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🔄 Complete User Journey Flow

### 1. Driver Authentication Flow
```
┌─────────────────┐
│ App Launch      │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Driver Login/   │
│ Selection       │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Store Driver ID │
│ for API calls   │
└─────────┬───────┘
          │
          ▼
    [Ready for Trip]
```

### 2. Trip Start Flow
```
┌─────────────────┐
│ Scan Start      │ ──► POST /api/scan-qr/
│ Terminal QR     │     {"qr_code": "terminal_id:XXX"}
└─────────┬───────┘
          │
          ▼ [QR Valid]
┌─────────────────┐
│ Show Terminal   │
│ Information     │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Select          │
│ Destination     │
│ Terminal        │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Enter Initial   │
│ Passenger Count │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Start Trip      │ ──► POST /api/trips/start/
│ (API Call)      │     {
└─────────┬───────┘       "driver_id": "XXX",
          │               "start_terminal": "XXX",
          │               "destination_terminal": "XXX",
          │               "passengers": 15
          ▼             }
    [Trip Active]
```

### 3. During Trip Flow
```
┌─────────────────┐
│ Trip Active     │
│ Screen          │
└─────────┬───────┘
          │
          ▼ [Passenger Change]
┌─────────────────┐
│ Update          │ ──► POST /api/trips/{id}/passengers/
│ Passenger Count │     {"passengers": 20}
└─────────┬───────┘
          │
          ▼ [API Success]
┌─────────────────┐
│ Update UI       │ ◄── Real-time sync with
│ Show New Count  │     Web Dashboard
└─────────┬───────┘
          │
          ▼ [Continue Trip]
    [Back to Active Screen]
```

### 4. Trip Completion Flow
```
┌─────────────────┐
│ Arrive at       │
│ Destination     │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Scan End        │ ──► POST /api/scan-qr/
│ Terminal QR     │     {"qr_code": "terminal_id:YYY"}
└─────────┬───────┘
          │
          ▼ [QR Valid]
┌─────────────────┐
│ Confirm Final   │
│ Passenger Count │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Complete Trip   │ ──► POST /api/trips/{id}/stop/
│ (API Call)      │     {"passengers": 18}
└─────────┬───────┘
          │
          ▼ [API Success]
┌─────────────────┐
│ Show Trip       │ ◄── Trip appears as
│ Completed       │     "Completed" in Dashboard
└─────────┬───────┘
          │
          ▼
    [Ready for New Trip]
```

## 📊 API Call Sequence Diagram

```
Mobile App          Django API          Firebase          Web Dashboard
    │                   │                   │                   │
    │ POST /scan-qr/     │                   │                   │
    ├──────────────────► │                   │                   │
    │ ◄──────────────────┤                   │                   │
    │   Terminal Info    │                   │                   │
    │                   │                   │                   │
    │ POST /trips/start/ │                   │                   │
    ├──────────────────► │ Create Trip       │                   │
    │                   ├─────────────────► │                   │
    │                   │ ◄─────────────────┤                   │
    │ ◄──────────────────┤ Trip Created      │ Real-time Update  │
    │   Trip ID          │                   ├─────────────────► │
    │                   │                   │                   │
    │ POST /passengers/  │                   │                   │
    ├──────────────────► │ Update Trip       │                   │
    │                   ├─────────────────► │                   │
    │                   │ ◄─────────────────┤                   │
    │ ◄──────────────────┤ Updated Trip      │ Real-time Update  │
    │   Updated Data     │                   ├─────────────────► │
    │                   │                   │                   │
    │ POST /trips/stop/  │                   │                   │
    ├──────────────────► │ Complete Trip     │                   │
    │                   ├─────────────────► │                   │
    │                   │ ◄─────────────────┤                   │
    │ ◄──────────────────┤ Completed Trip    │ Real-time Update  │
    │   Completion       │                   ├─────────────────► │
```

## 🎨 Screen Mockup Flow

### Screen 1: Driver Selection
```
┌─────────────────────────────┐
│ 🚐 Fleet Monitoring         │
├─────────────────────────────┤
│                             │
│ Select Driver:              │
│                             │
│ ○ Juan Dela Cruz            │
│ ○ Maria Santos              │
│ ○ Pedro Gonzales            │
│ ● Ana Rodriguez             │
│                             │
│ [Continue] [Settings]       │
└─────────────────────────────┘
```

### Screen 2: QR Scanner
```
┌─────────────────────────────┐
│ 📷 Scan Terminal QR Code    │
├─────────────────────────────┤
│                             │
│ ┌─────────────────────────┐ │
│ │                         │ │
│ │    [QR Scanner View]    │ │
│ │                         │ │
│ │    ┌─────────────┐      │ │
│ │    │             │      │ │
│ │    │   QR CODE   │      │ │
│ │    │             │      │ │
│ │    └─────────────┘      │ │
│ └─────────────────────────┘ │
│                             │
│ Point camera at terminal    │
│ QR code to scan             │
└─────────────────────────────┘
```

### Screen 3: Trip Setup
```
┌─────────────────────────────┐
│ 🚀 Start New Trip           │
├─────────────────────────────┤
│                             │
│ From: Dipolog Terminal      │
│ ✓ Scanned successfully      │
│                             │
│ To: [Select Destination]    │
│ ▼ Molave Terminal           │
│   Ozamiz Terminal           │
│   Pagadian Terminal         │
│                             │
│ Passengers: [15] [+] [-]    │
│                             │
│ [Start Trip] [Cancel]       │
└─────────────────────────────┘
```

### Screen 4: Active Trip
```
┌─────────────────────────────┐
│ 🚌 Trip in Progress         │
├─────────────────────────────┤
│                             │
│ From: Dipolog Terminal      │
│ To: Molave Terminal         │
│                             │
│ Started: 10:30 AM           │
│ Duration: 00:45:23          │
│                             │
│ Passengers: [20] [+] [-]    │
│                             │
│ [Update Count]              │
│ [Emergency Stop]            │
│ [Complete Trip]             │
└─────────────────────────────┘
```

### Screen 5: Trip Completion
```
┌─────────────────────────────┐
│ ✅ Trip Completed           │
├─────────────────────────────┤
│                             │
│ From: Dipolog Terminal      │
│ To: Molave Terminal         │
│                             │
│ Started: 10:30 AM           │
│ Completed: 11:15 AM         │
│ Duration: 00:45:00          │
│                             │
│ Final Passengers: 18        │
│                             │
│ [Start New Trip]            │
│ [View History]              │
└─────────────────────────────┘
```

## 🔄 Real-time Synchronization Flow

```
Mobile Action               Firebase Update              Dashboard Update
     │                           │                           │
     │ Start Trip                │                           │
     ├─────────────────────────► │ Create trip document      │
     │                           ├─────────────────────────► │ Show new trip
     │                           │                           │
     │ Update Passengers         │                           │
     ├─────────────────────────► │ Update passengers field  │
     │                           ├─────────────────────────► │ Update count
     │                           │                           │
     │ Complete Trip             │                           │
     ├─────────────────────────► │ Set status: "completed"  │
     │                           ├─────────────────────────► │ Move to completed
     │                           │ Set arrival_time          │ section
```

## 🚨 Error Handling Flow

```
API Call Failed
     │
     ▼
┌─────────────────┐
│ Check Error     │
│ Type            │
└─────────┬───────┘
          │
          ├─── Network Error ──► Show "Check connection" + Retry
          │
          ├─── 400 Bad Request ──► Show "Invalid data" + Fix form
          │
          ├─── 404 Not Found ──► Show "Item not found" + Go back
          │
          └─── 500 Server Error ──► Show "Server issue" + Try later
```

## 📱 Offline Handling Flow

```
┌─────────────────┐
│ User Action     │
└─────────┬───────┘
          │
┌─────────▼───────┐
│ Check Network   │
│ Status          │
└─────────┬───────┘
          │
          ├─── Online ──► Make API Call ──► Update UI
          │
          └─── Offline ──► Queue Action ──► Show "Queued"
                              │
                              ▼
                         ┌─────────────────┐
                         │ Connection      │
                         │ Restored        │
                         └─────────┬───────┘
                                   │
                                   ▼
                              Process Queue ──► Sync All Actions
```

---

**🎯 Key Success Metrics:**
- Trip start appears in dashboard within 2 seconds
- Passenger updates sync in real-time
- Error rate < 1% for API calls
- 100% data consistency between mobile and web
